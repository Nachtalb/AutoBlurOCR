{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Redakt main window. No network, no filesystem plugin: all I/O goes through commands.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-set-title"]
}
