{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "AutoBlur main window. No network, no filesystem plugin: all I/O goes through commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
